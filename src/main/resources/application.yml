spring:
  profiles:
    active:
  #Mysql
  datasource:
    url: jdbc:mysql://127.0.0.1:3306/exam_online?serverTimezone=UTC
    username: root
    password:
    driver-class-name: com.mysql.cj.jdbc.Driver
  #redis
  redis:
    host: 118.178.253.94
server:
  port: 8088
#  Swagger2
swagger:
  enable: true
# 打印Sql日志
mybatis-plus:
  configuration:
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl
  #  开启驼峰映射
    map-underscore-to-camel-case: true
  #  用了@MapperScan可以不配置接口包，但要配置编译出来的.xml文件位置
  mapper-locations: classpath*:com/march/main/mapper/*.xml